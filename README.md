# Python parser NNEF

> Used as Temporary Fix

borrowed from:
```
git@github.com:KhronosGroup/NNEF-Tools.git&subdirectory=./parser/python
```
the 2022-03-17

Allow to avoid limitation in poetry package manager to handle multiple subdirectory from
same git repo.
